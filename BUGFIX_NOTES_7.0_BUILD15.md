# Fodd 7.0 Personal Team — Build 15 Bugfix

Fixed Xcode compile errors reported on 29 Aug 2026:

- Replaced unsupported `UTType.event` Spotlight content type with `.content`.
- Made the shared Fodd brand palette accessible across Swift files.
- Made `PremiumCard` / `premiumCard()` accessible to `AppleExperience.swift`.
- Retained Personal Team mode, bundle id `com.meruartama.fodd`, and Railway API URL `https://fodd-ios-production.up.railway.app/`.
- Build number bumped from 14 to 15.
